# 2. BE — Database & API

## Time blocks

- **9:20–9:35** — Supabase project, run the **two-table SQL**
  (reports + challenges minimal), share URL + anon key **privately** with INT and FE.
- **9:35–10:00** — `POST /api/reports`: insert report → call `compile()` →
  create challenge (or increment `report_count` if same category + district
  exists in last 30 days) → return both.
  `GET /api/challenges` sorted by priority desc.
- **10:00–10:10** — Test both routes on the **deployed URL**, not localhost.

## Done when

Posting from a phone creates a row that the list endpoint returns.

## Line at 10:30

> "Real Postgres, real rows. Here's the record that was just created."

## Don't

Add auth, row-level security, or any other table yet.

## Lives in repo

- Migrations → `backend/supabase/migrations/`
- Route handlers → `frontend/app/api/` (thin), logic → `backend/src/lib/`
