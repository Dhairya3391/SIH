# 3. AI — Compiler & Words

## Time blocks

- **9:20–9:35** — Write `compile()` in `lib/compile.ts`, export it, hand to BE.
  **Only blocking task — do it first.** Rule-based keyword classifier only:
  input text → `{ title, category, severity, priority, people_est }`.
- **9:35–10:00** — Write **10 report sentences that sound real**:
  5 Hindi (Roman script fine), 5 English, **2 near-duplicates of the lightning one**.
  Give to INT for seed.
- **10:00–10:10** — Write the "what's next" wording: rule-based now →
  same function calls Claude by H12 → dedup by embeddings after.

## Done when

Any sentence typed into the form produces a sensible title, category, priority.

## Line at 10:30

> "It's rule-based right now. Real AI plugs in behind the same function by hour 12."

## Don't

Call an LLM, install any AI SDK, or touch embeddings before 10:30.

## Lives in repo

- `backend/src/lib/fallback.ts` (now) → `llm.ts` (H12) · seed sentences → `backend/supabase/seed/`
