# 4. FE — Report Screen & Queue

## Time blocks

- **9:20–9:30** — Page `/report`: one text box, photo input,
  "use my location" button, people-affected number, Submit.
  Big controls, works on a phone.
- **9:30–9:50** — Page `/queue`: list from `GET /api/challenges`;
  each row = title, district, category, priority chip.
  Use mock array until BE route is live.
- **9:50–10:05** — Wire both to real API. Add `submitting…` state + error message.
- **10:05–10:10** — Test on your own phone via deployed URL.

## Done when

Phone submit → queue shows new item at top after refresh.

## Line at 10:30

You drive the demo. Quote: "No form-filling, no English, and it's on a phone."

## Don't

Build challenge detail page, routing guards, or animations.

## Lives in repo

- `frontend/app/report/`, `frontend/app/queue/`, `frontend/lib/api.ts`
