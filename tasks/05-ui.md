# 5. UI — Must Look Like a Real Product

## Time blocks

- **9:20–9:35** — Tokens in `globals.css`: ink `#102027`, ground `#F4F6F5`,
  teal `#2E7180`, critical `#D94F45`, high `#E07B2E`, moderate `#E5A83B`,
  resolved `#3E8064`, line `#CCD1C7`. Load **Mukta** from Google Fonts.
- **9:35–9:55** — Priority chip (colour by band **plus the word**, never colour
  alone), card, top bar with JharSetu wordmark + fake role dropdown
  (Citizen / Coordinator) that just switches page.
- **9:55–10:10** — `/queue` as ops console: **"Needs immediate action"** strip
  (top-3 priority) first, then the rest.

## Done when

A stranger can tell what is urgent in two seconds.

## Line at 10:30

> "Colour always carries a word too, because red and green look the same to many people."

## Don't

Build map, charts, or landing page.

## Lives in repo

- `frontend/app/globals.css`, `frontend/components/` (chip, card, topbar)
